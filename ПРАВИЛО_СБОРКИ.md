# ПРАВИЛО СБОРКИ — атом → клетка → орган → организм

> Закреплено 2026-07-13 (биологическая иерархия, согласована с автором).
> Код: `ядро/кирпич_момент.py`, `ядро/кресты.py`, `ядро/оси.py`,
> `scripts/библиотека_кирпичей.py`, `scripts/собрать_новое.py`.
> Машиночитаемая копия — в `данные/библиотека_кирпичей/манифест.json` → `правило_сборки`.

## Непреложные инварианты

- **Ничего не нормируем** (ни среднее/σ, ни медиана) — только реальные величины.
- **Ничего не сливаем** в одно число — каждый параметр отдельно, реальный.
- **Ничего не режем/не сглаживаем.**
- **Детерминизм:** один вход → бит-в-бит один выход.

## Живая иерархия (как в теле)

```
ОРГАНИЗМ  (вся мелодия — композиция органов)
   └─ ОРГАН  (одно явление/голос: дождь, буква, раскат)
        └─ КЛЕТКА  (локальное событие: слог, капля, порыв, формантный сгусток)
             └─ АТОМ  (момент 46 мс — неделимое зерно)
                   ВНУТРИ АТОМА — ВСЁ: данные живого ЗВУКА + живого ОБРАЗА
```

Группировка снизу вверх — **по крестам** (связям резонанса time/freq/harmonic,
`ядро/кресты.py`): сильные кресты → клетка; связи клеток → орган.

---

## АТОМ — момент 46 мс = протон + электрон + образ_кусочек

Канон: `СТРОЕНИЕ_АТОМА_МОМЕНТА.md` · код: `ядро/строение_атома.py`.

Три обязательных слота (без любого атом неполон):

- **ПРОТОН** — спектр mag+phase (`.npz`) + **61** ключ
  (SPECTRAL / VOCAL / MUSICAL / PERCEPTUAL / mfcc / zcr).
- **ЭЛЕКТРОН** — **47** ключей (TEMPORAL / SPATIAL / MOVEMENT / mfcc_delta / AXES);
  при моно `spatial_статус = н/п_моно`.
- **ОБРАЗ_КУСОЧЕК** — точки живого образа в **тот же** `t` / `старт`;
  цвет из `палитры_образа`, **НЕ** freqToHue.

Метки: `t`, `старт`. Кресты — связи с соседями.
Окно = **1024** (~46 мс), шаг 512; хвост добивается нулями до сетки hop.
Сборка архива: `python3 scripts/библиотека_кирпичей.py` → `атомов_полных` в манифесте.

Приёмка corr: `python3 scripts/cp_моментов.py`.  
Две системы: `ДВЕ_СИСТЕМЫ_КИРПИЧЕЙ.md`.

---

## КЛЕТКА — группа атомов по сильным крестам

Локальное живое событие. Атомы, связанные резонансом ≥ τ_клетка, — одна клетка
(связная компонента). Ориентир: ~12–16 клеток на звук (τ≈0.85).

## ОРГАН — группа клеток (одно явление/голос)

Клетки, связанные крестами, — один орган. Один природный звук/буква = один орган.
Библиотека = набор органов (55).

## ОРГАНИЗМ — вся мелодия

Несколько органов, собранных во времени. Пример: ШТОРМ = орган дождя + орган
грома (`scripts/собрать_новое.py`). Новый звук/мелодия — организм из органов.

---

## СБОРКА (правила)

### звук → атомы → клетки → орган
`в_кирпичи(x)` → атомы (звук+форма+кресты); группировка по крестам → клетки → орган.
Оси клетки/органа (fd, mod_depth, mod_rate…) — из потока (`ядро/оси.py`), не из атома.

### атомы → звук  (образ→звук)
`из_кирпичей(атомы)` — overlap-add **полного спектра**, деление на Σw.
- Собирать из спектра, НЕ из пиков. `старт` — из манифеста, не из `t`.
- Проверка: буквы corr=1.0; шум corr 0.993–0.999.

### атомы → образ  (звук→образ)
Образ организован ПО ЧАСТОТЕ (живой образ V3), атомы — по ВРЕМЕНИ. Правило:
- **разворачиваем спектр каждого атома по частоте**: каждый ПИК атома → точка
  `X=время атома`, `Y=log(частота пика)`, `размер=амплитуда пика`;
- **цвет = из ПАЛИТРЫ ОРГАНА** (`палитра_образа` в манифесте: частота→RGB атомов
  живой клетки V3, интерполяция). НЕ freqToHue, НЕ вычислять из звука.
- НЕЛЬЗЯ: одна точка на атом (на центроиде) — схлопывает спектр, образ врёт
  (дождь выходил жёлтым вместо сине-зелёного).

### орган/организм → звук
Сумма органов по времени (тип≠место: сдвиг старта — параметр сборки).
Код: `ядро/организм.py` (`собрать_организм` / рецепты).
CLI: `python3 scripts/собрать_новое.py` → `выход/новое/*.wav`
Канон «гром+дождь=шторм»; также костёр, вьюга, пульс, вздох_а.

### замкнутость (канон автора)
Один набор параметров создаёт и сцену, и образ, и из образа собирается звук.
Параметры ↔ сцена ↔ образ ↔ звук — согласованы.
