# Алфавит — пересечение пакетов (v0.1 refresh)

> 2026-08-10 · правило: спека = пересечение файлов, не выдумка

## Корпуса

| Стихия | n | E | alignment | событие | закон | материя |
|--------|---|---|-----------|---------|-------|---------|
| дождь_live | 40 | PASS | `single_live_clip` | `drop_impact` | `particle_scale_impulse` | `drop_streak_impact_crown` |
| огонь | 7945 | PASS | `single_live_clip` | `combustion_crackle` | `buoyancy_rise_crackle` | `flame_tongue_crackle_spark` |
| ветер | 19 | PASS | `single_live_clip` | `wind_stream_rustle` | `horizontal_flow_sway` | `broken_fiber_fleck` |
| водопад | 19 | PASS | `single_live_clip` | `waterfall_fall_spray` | `gravity_fall_spray` | `vertical_ribbon_hard_spray` |
| река | 51 | PASS | `single_live_clip` | `river_channel_flow` | `channel_flow_current` | `current_filaments_foam` |
| гром | 6380 | PASS (delay долг) | `axes_decoupled_flash` | `flash_shockwave` | `shockwave_delay` | `flash_then_shock_field` |

## Пересечение корня атома

```
alignment · birth · id · t_sec_причина · звук_ядро · кресты · образ_причины · обратимость · стихия
```

## Пересечение `образ_причины`

```
закон · не_есть · рендер · событие · фаза
```

## Пересечение `закон`

```
energy_rel · size_atom · size_med · модель · формула
```

## Пересечение `рендер`

```
запрещено · запрещено_материя · материя · тип
```

## Пересечение `звук_ядро.ядро`

```
amp · freq · harmonicity · lifetime · size
```

## Вывод

- Инвариант корня стабилен на 6 пакетах.
- В `рендер` теперь обязательно: `материя` + `тип` (+ `запрещено_материя`).
- В `закон` пересечение: `модель` · `формула` · `energy_rel` · `size_atom` · `size_med`.
- Слоты события (полёт/удар/пламя/…) — **не** общий shape.
- Гром: E PASS, но `delay_s` — постоянный долг.

JSON: `отчёты/алфавит_пересечение.json`
