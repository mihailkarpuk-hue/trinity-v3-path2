# КАНОН: ПОЛНЫЙ КРУГ ЗВУК ↔ ОБРАЗ (Тринити V3 → V3.1)

> Канон архитектуры. Дата фиксации: **2026-07-05**.
> Формула: **звук(образ) = скелет(геометрия) ⊗ плоть(ближайший чистый образ)**
> Статус: целевая архитектура V3.1. Ниже — сам канон и карта наложения на текущий код V3.

---

## СХЕМА

```
        ЗВУК                                        ОБРАЗ
          │                                           ▲
   1  АТОМИЗАЦИЯ                              5  ПРОЕКЦИЯ
   треки: тон/шум/переход                     X=время Y=log f Z=amp
          │                                   цвет=спектр, размер=энергия
          ▼                                           │
   2  ГЕН каждого атома ──────────────────────────────┘
   (порождающий код)
          │
   3  ФЕНОТИП (108) ──► 4 УЗНАВАНИЕ
                        сопоставление РАСПРЕДЕЛЕНИЙ
                        атомов с банком чистых образов
                        (18 базис + 37 живых)
                              │
                              ▼
                     ЧИСТЫЙ ОБРАЗ-ДОНОР
                    (архетип: дождь, огонь,
                     капля, буква, сердце...)

   ═══════════ ОБРАТНАЯ ДОРОГА ═══════════

        ЛЮБОЙ ОБРАЗ (3D-форма, рисунок, облако точек)
          │
   6  ОБРАТНАЯ ПРОЕКЦИЯ (геометрия → атомы-скелеты)
   точка → (birth, freq, amp); кривизна → шумность;
   плотность точек → onset_density; цвет → наклон спектра
          │
   7  ТЕМБРО-ДОНОРСТВО
   узнать ближайший чистый образ ЭТОЙ геометрии →
   его ГЕН-статистики (harmonicity, fd, mod_rate,
   формы огибающих) одевают скелет плотью
          │
   8  ГРАНУЛЯРНЫЙ СИНТЕЗ (маппинг_104 → GrainSpec)
          │
          ▼
        ЗВУК ОБРАЗА
```

---

## ПЯТЬ ПРИНЦИПОВ

1. **Разрез ГЕН / ФЕНОТИП.** ГЕН — порождающие параметры (из них звук собирается). ФЕНОТИП — описательные 108 (по ним звук узнаётся). Обратимость идёт **только через ГЕН**.
2. **Атом — трек, не кадр.** Атом несёт траектории `freq(t)`, `amp(t)`: чирп, форму огибающей, честный `lifetime`. Событие (капля) и состояние (гласная) различаются **на уровне данных**.
3. **Узнавание — по распределениям атомов, не по средней точке.** Банк чистых образов (18 базис-эталонов + 37 живых букв) — приоритетный пул, «фонетический алфавит природы».
4. **Обратная дорога — через донора.** Геометрия образа даёт **партитуру** (когда / какой высоты / какой громкости), ближайший чистый образ даёт **инструмент** (тембр, ген).
5. **Критерий честности круга:** `ε = d(фенотип(звук), фенотип(синтез(образ(звук))))` — расстояние в метрике узнавания между паспортом **до** и **после** полного круга.

---

## КАРТА НАЛОЖЕНИЯ НА ТЕКУЩИЙ КОД V3

Легенда: 🟢 есть · 🟡 частично / требует переработки · 🔴 строить с нуля.

| # | Шаг канона | Статус | Где сейчас / что менять |
|---|---|---|---|
| 1 | **Атомизация в треки** | 🟡 | `scripts/пересборка_живых_букв.py::атомизировать()` даёт **покадровые** атомы (один freq/amp на кадр, `lifetime` от насадки на форму). Принцип 2 требует **треков**: связать кадры в траектории `freq(t)`/`amp(t)`, честный `lifetime`, различать событие/состояние. → **переработка ядра атомизации.** |
| 2 | **ГЕН каждого атома** | 🟡 | `ядро/маппинг_104.py::операторы_гранулы()` → `GrainSpec` уже есть (порождающие: ADSR, jitter/shimmer, noise=f(fd), band). Но **разрез ГЕН/ФЕНОТИП не выделен явно** в данных. → зафиксировать ГЕН как отдельный слой атома, отдельно от `params_104`. |
| 3 | **Фенотип (108)** | 🟢 | `ядро/паспорт_атома.py` + `обогатить_атомы_104.py`. 108 измерений, 55 файлов с per-atom паспортом. Готово. |
| 4 | **Узнавание по распределениям** | 🟡🔴 | `приложение/общее/узнавание.js` сейчас = **средняя точка** в 108D (евклид/√108). Принцип 3 требует: (а) сравнение **распределений** атомов (EMD/MMD/гистограммы), (б) **приоритетный пул** 18+37 чистых образов. → новая метрика + двухступенчатое узнавание. Это корень бага «капля→гласные». |
| 5 | **Проекция (образ)** | 🟢 | `приложение/блоки/просмотр_клеточки/проекция_образ.js`: X=время, Y=log f, Z=amp, цвет=color_rgb/Гц, размер=энергия, пульс от живого спектра. Готово. |
| 6 | **Обратная проекция (геометрия→скелеты)** | 🔴 | Кода нет. Нужен модуль: 3D-форма / рисунок / облако точек → атомы-скелеты (точка→birth/freq/amp; кривизна→шумность; плотность→onset_density; цвет→наклон спектра). |
| 7 | **Тембро-донорство** | 🔴 | Концепт новый. `узнавание` умеет найти ближайшего, но «одеть скелет ГЕН-статистиками донора» (harmonicity, fd, mod_rate, формы огибающих) не построено. Зависит от §4 (метрика) и §2 (слой ГЕН). |
| 8 | **Гранулярный синтез** | 🟢🟡 | `ядро/маппинг_104.py` + `ядро/синтез_104.py` / `синтез.py` существуют (overlap-add, GrainSpec). → нужно **подключить** вход «скелет + ГЕН донора», а не только «клетка→звук». |
| ε | **Критерий честности круга** | 🔴 | Не измеряется. Станет вычислимым, как только §4 даст стабильную метрику узнавания. Первый честный тест круга. |

---

## ПОРЯДОК СБОРКИ V3.1 (предлагаемый, от корня напряжения)

1. **§4 Метрика узнавания по распределениям + приоритетный пул** — снимает баг «капля→гласные», даёт метрику для ε. *(прямое продолжение крайней работы над узнаванием)*
2. **§1 Атом-трек** — без честных траекторий распределения бедны; событие ≠ состояние на уровне данных.
3. **§2 Слой ГЕН** — явно отделить порождающие параметры от 108-фенотипа.
4. **ε-замер прямого круга** (звук→образ→узнавание) — измерить честность до обратной дороги.
5. **§6 Обратная проекция** и **§7 донорство** и **§8 подключение синтеза** — обратная дорога образ→звук.

---

## ССЫЛКИ

- Полный разбор текущего V3: `ТРИНИТИ_V3_АНАЛИЗ_ДЛЯ_CHAT.md`
- Узнавание (крайнее): `приложение/общее/узнавание.js`, `построить_индекс_108.py`, `данные/узнавание_индекс_108.json`
- Паспорт/ГЕН: `ядро/паспорт_атома.py`, `ядро/маппинг_104.py`, `ядро/оси.py`, `ядро/фаза.py`
- Атомизация/синтез: `scripts/пересборка_живых_букв.py`, `ядро/синтез_104.py`, `ядро/синтез.py`
```
