# Запуск сервера Тринити V3 (для code / Терминала)

Сервер: `СЕРВЕР.py` → порт **8004**. Скрипт сам переходит в свою папку
(`os.chdir`), поэтому его можно запускать откуда угодно по полному пути.

## Вариант A — code находит сервер сам и запускает (рекомендую)

```bash
# освободить порт 8004, если занят прошлым запуском
lsof -ti tcp:8004 | xargs kill -9 2>/dev/null
# найти СЕРВЕР.py именно от V3 и запустить
SRV=$(find ~ -path "*Тринити_V3/СЕРВЕР.py" 2>/dev/null | head -1)
echo "Запускаю: $SRV"
python3 "$SRV"
```

Затем открыть в браузере: **http://localhost:8004/**

## Вариант B — прямой путь (если он известен)

```bash
lsof -ti tcp:8004 | xargs kill -9 2>/dev/null
python3 "/ПОЛНЫЙ/ПУТЬ/Тринити Атомы Звука/Тринити_V3/СЕРВЕР.py"
```

## Вариант C — двойной клик

Файл `ЗАПУСТИТЬ.command` (порт 8004). Если macOS пишет «нет прав доступа» —
в Терминале один раз: `chmod +x "/путь/Тринити_V3/ЗАПУСТИТЬ.command"`,
затем двойной клик. Если Gatekeeper блокирует — Системные настройки →
Конфиденциальность → «Всё равно открыть».

## Проверка, что сервер жив

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8004/
```
Ожидаемо: `200`.

## Заметки

- Сервер не освобождает терминал — он работает на переднем плане. Остановить: Ctrl+C.
- Чтобы запустить в фоне: добавить `&` в конце команды python3.
- V2 (порт 8003) и V3 (8004) можно держать запущенными одновременно.
- Если изменили файлы и браузер показывает старое — жёсткое обновление Cmd+Shift+R
  (сервер уже отдаёт `Cache-Control: no-store`).
