# HANDOFF · Trinity V3 · Путь 2 (для GPT / Claude / Grok)

**Скоуп:** разбор пути 2. Систему A (словарь CP#2 / d₉) **не трогать**.

## Задача

1. Разобрать архитектуру: атом → клетка → орган → организм; `библиотека_природы`; **буква ≠ картина**.
2. Предложить **правило пазла**: как из атомов собирать **узнаваемую картину** (не россыпь точек и не ручной фотомонтаж).

## Инварианты пути 2

- Атом ~46 мс = **протон (61) + электрон (47) + образ_кусочек** (слот обязателен).
- Носитель звука: mag+phase в `.npz`; параметры в JSON.
- Тип ≠ место: сдвиг во времени — параметр сборки.
- Ничего не нормировать / не сливать в одно число / детерминизм.
- Образ синхронен звуку (`t` / `старт`); цвет **не** freqToHue.

## Полки

| Полка | Содержание |
|-------|------------|
| `данные/библиотека_кирпичей/` | B: ~3055 атомов, 55 органов (голос/корпус), corr PASS |
| `данные/библиотека_природы/` | 7 стихий, ~4209 атомов; звук из video_live; образ из кадра |
| `данные/стихии_живые/` | сырьё: video_live + clean_video |
| `данные/алфавит_природы.json` | индекс букв = одиночные атомы |

## Буква ≠ картина

- **Буква** = один атом (момент + параметры + частички образа).
- **Картина** = сборка многих кусочков в **новое узнаваемое целое**.
- Статус проверки: звук сборки «лесная гроза» терпим; образ из точек — ерунда; фотомонтаж live-кадров — «похоже», но это ещё не правило пазла из атомов.

## Ключевые файлы

- `ПРАВИЛО_СБОРКИ.md`, `СТРОЕНИЕ_АТОМА_МОМЕНТА.md`, `ДВЕ_СИСТЕМЫ_КИРПИЧЕЙ.md`
- `ядро/кирпич_момент.py`, `ядро/строение_атома.py`, `ядро/организм.py`, `ядро/частички_кадра.py`
- `scripts/собрать_картину_природы.py` (если есть), `приложение/картина/`, `приложение/буква/`

## Просьба к модели

Дай: (a) схему пути 2 as-is, (b) дыры, (c) конкретное правило пазла «атомный кусок → узнаваемая картина» с критерием приёмки, без правок системы A.
